---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: base64controllers.kubernetes-base64-encode-controller.my.domain
spec:
  group: kubernetes-base64-encode-controller.my.domain
  names:
    kind: Base64Controller
    listKind: Base64ControllerList
    plural: base64controllers
    singular: base64controller
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Base64Controller is the Schema for the base64controllers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Base64ControllerSpec defines the desired state of Base64Controller
            properties:
              name:
                description: Foo is an example field of Base64Controller. Edit base64controller_types.go
                  to remove/update
                type: string
              text:
                type: string
            type: object
          status:
            description: Base64ControllerStatus defines the observed state of Base64Controller
            properties:
              encodedText:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
